name: pr-workflows
on:
    push:
    pull_request:
        types:
            - opened
            - closed
jobs:
    pr-actions-workflow:
        runs-on: ubuntu-latest
        steps:
            - name: workflow run
              run: echo "workflow is running"
